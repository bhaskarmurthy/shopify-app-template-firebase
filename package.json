{
  "name": "firebase-shopify-app",
  "version": "1.0.0",
  "description": "Example Shopify app using Firebase",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "start": "turbo run serve",
    "deploy": "turbo run deploy",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
